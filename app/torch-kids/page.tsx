import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Torch Kids - Torch Fellowship',
  description: "Children's ministry at Torch Fellowship.",
};
import PageClient from '@/app/_pages/TorchKidsPage';
export default PageClient;
