import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Contact â" Torch Fellowship',
  description: 'Get in touch with Torch Fellowship.',
};
import PageClient from '@/app/_pages/ContactPage';
export default PageClient;
