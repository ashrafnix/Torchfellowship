import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Blog â" Torch Fellowship',
  description: 'Articles, news, and updates from Torch Fellowship.',
};
import PageClient from '@/app/_pages/BlogListPage';
export default PageClient;
