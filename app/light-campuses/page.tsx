import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Light Campuses â" Torch Fellowship',
  description: 'Find a Torch Fellowship Light Campus near you.',
};
import PageClient from '@/app/_pages/LightCampusesPage';
export default PageClient;
