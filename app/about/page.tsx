import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'About - Torch Fellowship',
  description: "Learn about Torch Fellowship's mission, vision, and history.",
};
import PageClient from '@/app/_pages/AboutPage';
export default PageClient;
