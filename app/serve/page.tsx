import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Serve â" Torch Fellowship',
  description: 'Join a ministry team and serve at Torch Fellowship.',
};
import PageClient from '@/app/_pages/ServePage';
export default PageClient;
