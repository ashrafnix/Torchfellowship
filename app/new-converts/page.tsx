import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'New Converts â" Torch Fellowship',
  description: 'New to faith? Start your journey here.',
};
import PageClient from '@/app/_pages/NewConvertsPage';
export default PageClient;
