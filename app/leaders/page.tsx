import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Leaders â" Torch Fellowship',
  description: 'Our leaders at Torch Fellowship.',
};
import PageClient from '@/app/_pages/LeadersPage';
export default PageClient;
