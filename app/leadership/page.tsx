import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Leadership â" Torch Fellowship',
  description: 'Meet the leadership team at Torch Fellowship.',
};
import PageClient from '@/app/_pages/LeadershipPage';
export default PageClient;
