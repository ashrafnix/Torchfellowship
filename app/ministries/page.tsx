import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Ministries â" Torch Fellowship',
  description: 'Explore the ministries at Torch Fellowship.',
};
import PageClient from '@/app/_pages/MinistriesPage';
export default PageClient;
