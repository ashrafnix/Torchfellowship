import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Sign Up â" Torch Fellowship',
  description: 'Create your Torch Fellowship account.',
};
import RegisterPageClient from '@/app/_pages/RegisterPage';
export default RegisterPageClient;
