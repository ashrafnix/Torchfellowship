import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Communities â" Torch Fellowship',
  description: 'Join a community group at Torch Fellowship.',
};
import PageClient from '@/app/_pages/CommunitiesPage';
export default PageClient;
