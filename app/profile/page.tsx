import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'My Profile â" Torch Fellowship',
  description: 'Manage your Torch Fellowship account.',
};
import PageClient from '@/app/_pages/ProfilePage';
export default PageClient;
