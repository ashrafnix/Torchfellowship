import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Events â" Torch Fellowship',
  description: 'Upcoming events and gatherings at Torch Fellowship.',
};
import PageClient from '@/app/_pages/EventsPage';
export default PageClient;
