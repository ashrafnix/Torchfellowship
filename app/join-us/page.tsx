import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Join Us â" Torch Fellowship',
  description: 'Become part of the Torch Fellowship family.',
};
import PageClient from '@/app/_pages/JoinUsPage';
export default PageClient;
