import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Give â" Torch Fellowship',
  description: 'Support Torch Fellowship through giving.',
};
import PageClient from '@/app/_pages/GivePage';
export default PageClient;
