import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Community Chat â" Torch Fellowship',
  description: 'Connect with the Torch Fellowship community.',
};
import PageClient from '@/app/_pages/ChatPage';
export default PageClient;
