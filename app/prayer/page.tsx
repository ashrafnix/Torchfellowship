import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Prayer â" Torch Fellowship',
  description: 'Submit a prayer request or pray for others.',
};
import PageClient from '@/app/_pages/PrayerPage';
export default PageClient;
