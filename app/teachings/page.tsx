import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Teachings â" Torch Fellowship',
  description: 'Watch and listen to sermons and teachings from Torch Fellowship.',
};
import PageClient from '@/app/_pages/TeachingsPage';
export default PageClient;
